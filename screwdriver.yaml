jobs:
    main:
        annotations:
            screwdriver.cd/buildPeriodically: '*/2 * * * *'
        description: 'hello I am the first job that will runnnn'
        requires: [~pr, ~commit]
        image: node:8
        steps:
            - echo: |
                cat $SD_SOURCE_DIR/ascii-art.txt
            - cat: cp -a /sd/meta/. $SD_ARTIFACTS_DIR/meta/
    
